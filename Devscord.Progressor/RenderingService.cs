using System;
using System.Collections.Generic;
using System.Text;

namespace Devscord.Progressor
{
    internal class RenderingService
    {
        private readonly ResultImageConfiguration _resultImageConfiguration;

        internal RenderingService(ResultImageConfiguration resultImageConfiguration)
        {
            this._resultImageConfiguration = resultImageConfiguration;
        }

        internal void RenderBar(ChartData chartData)
        {

        }
    }
}
